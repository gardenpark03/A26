# Archive 26 - Make 2026 Count

2026년의 모든 순간을 기록하고, 목표를 달성하고, 성장을 추적하는 웹 애플리케이션입니다.

## 🚀 시작하기

### 필요 사항
- Node.js 18.17 이상
- npm 또는 yarn

### 설치 및 실행

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 프로덕션 빌드
npm run build

# 프로덕션 서버 실행
npm start
```

개발 서버를 실행한 후 [http://localhost:3000](http://localhost:3000)에서 확인할 수 있습니다.

## 📁 프로젝트 구조

```
A26/
├── app/                        # Next.js App Router 디렉토리
│   ├── layout.tsx             # 루트 레이아웃
│   ├── page.tsx               # 메인 페이지 (/)
│   ├── globals.css            # 전역 스타일
│   └── dashboard/
│       └── page.tsx           # 대시보드 페이지
├── components/                 # React 컴포넌트
│   ├── header.tsx             # 헤더 컴포넌트
│   └── ui/                    # shadcn/ui 컴포넌트
│       ├── button.tsx
│       ├── card.tsx
│       └── input.tsx
├── lib/
│   └── utils.ts               # 유틸리티 함수
├── tailwind.config.ts         # Tailwind CSS 설정
├── postcss.config.js          # PostCSS 설정
├── tsconfig.json              # TypeScript 설정
├── next.config.js             # Next.js 설정
├── components.json            # shadcn/ui 설정
└── package.json               # 프로젝트 의존성
```

## 🛠️ 기술 스택

- **Framework:** Next.js 14 (App Router)
- **Language:** TypeScript
- **Styling:** Tailwind CSS
- **UI Components:** shadcn/ui
- **Font:** Inter (Google Fonts)

## ✨ 주요 기능

- ✅ 반응형 디자인
- ✅ 다크모드 지원 준비
- ✅ shadcn/ui 컴포넌트 시스템
- ✅ TypeScript로 타입 안정성 확보
- ✅ Tailwind CSS로 빠른 스타일링

## 📄 페이지 구성

### 홈 페이지 (/)
- Archive 26 소개
- 주요 기능 카드
- 대시보드로 이동

### 대시보드 (/dashboard)
- 통계 위젯 (목표, 달성률, 기록, 활동 점수)
- 최근 활동 내역
- 진행 중인 목표

## 🎨 커스터마이징

### 색상 테마
`app/globals.css` 파일에서 CSS 변수를 수정하여 색상 테마를 변경할 수 있습니다.

### 컴포넌트
`components/ui/` 디렉토리의 shadcn/ui 컴포넌트를 자유롭게 커스터마이징할 수 있습니다.

## 📝 라이선스

MIT

